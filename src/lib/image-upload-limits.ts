/** Keep under Next’s default 1 MB Server Action limit (+ form overhead). */
export const PRODUCT_IMAGE_MAX_BYTES = 900 * 1024;
export const LOGO_IMAGE_MAX_BYTES = 900 * 1024;
export const GALLERY_IMAGE_MAX_BYTES = 900 * 1024;

export const PRODUCT_IMAGE_HINT =
  "JPEG, PNG, or WebP · large photos are resized automatically";
